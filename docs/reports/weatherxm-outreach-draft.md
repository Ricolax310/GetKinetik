# WeatherXM outreach — draft (use with `weatherxm-sybil-report.md` attached or linked)

This file is internal — pick the version that matches the channel you actually
send through. Keep it short. Don't editorialize the findings: the report does
that for us, in WeatherXM's own vocabulary.

---

## Suggested channels (in order)

1. **WeatherXM Discord** — DM a core team member if you can; otherwise post in
   an integrations or data channel.
2. **`info@weatherxm.com`** — general inquiry, but slower.
3. **X / Twitter DM** — if you have an X Premium DM lane and know a team
   handle. Keep it polite, link to the report on GitHub.
4. **GitHub Discussions** on `weatherxm/wxm-android` — public, sometimes
   higher-signal than support email.

Don't blast all four. Pick one, wait 48 hours.

---

## Short version — DM / email body

> Subject: Public-data Sybil scan on the WeatherXM Network — wanted to share before we publish anything
>
> Hi <name>,
>
> Eric here, building GETKINETIK (independent bureau for DePIN devices —
> hardware-signed identity + a public Genesis Score). We ran a Sybil-shape scan
> against your public `/api/v1/cells` endpoint earlier today. No internal data
> from you was used.
>
> Two specific findings worth a look before we post anything:
>
> 1. **290 cells (~3.4% of the network) sit ≥1.5× over designed capacity.**
> 2. In the **40** most over-capacity cells we drilled into, **60 of 112
>    devices** carry your own `pol_reason: NO_LOCATION_DATA` flag — the
>    network's own pipeline is already calling them out. Two hexes in
>    southwest Arkansas / east Texas each show **10 inactive devices in a
>    capacity-1 cell**, all sharing the same UUID-v1 batch prefix and the
>    same `WS1001 / WG1200` bundle. That has the shape of one operator.
>
> Full report (markdown, one page):  
> https://github.com/Ricolax310/GetKinetik/blob/main/docs/reports/weatherxm-sybil-report.md
>
> The scan was generated by `scripts/sybil-scan-weatherxm.mjs` — runnable in
> 60 seconds, no auth, only the public cells endpoint. Happy to walk you
> through how the bureau computes Genesis Scores for verified GETKINETIK
> nodes if that's useful to your operator-trust work; equally happy to just
> hand over the report and disappear.
>
> Not selling anything; the bureau API
> (`https://getkinetik.app/api/verify-device`) is free for the first wave.
>
> — Eric
> eric@outfromnothingllc.com · https://getkinetik.app/bureau/

---

## Shorter version — X DM (≤500 chars)

> Hi — Eric / GETKINETIK (independent bureau for DePIN nodes). We ran a
> Sybil-shape scan on the public WeatherXM cells API today. 290 cells over
> designed capacity, and in the top 40 we drilled, 60 of 112 devices carry
> your own `NO_LOCATION_DATA` flag. Two hexes near Arkansas/Texas show 10
> inactive devices each with the same UUID batch + bundle. Wanted to share
> before we post anything. Report:
> https://github.com/Ricolax310/GetKinetik/blob/main/docs/reports/weatherxm-sybil-report.md

---

## Pull-quote bank (use if/when they reply or you post publicly)

- "WeatherXM's own pipeline is already flagging these devices —
  `pol_reason: NO_LOCATION_DATA` is the network's flag, not ours. The bureau
  is just counting them."
- "We deliberately drill only the most over-capacity cells so every row in
  the device table is auditable by hand."
- "If WeatherXM wants per-device Genesis Scores, every device that also runs
  GETKINETIK can be POSTed to `/api/verify-device` for a hardware-rooted
  grade — neutral, no fee for first-wave partners."

---

## What NOT to do

- Don't blast this to a Slack channel without naming a person.
- Don't claim fraud. Use "shape," "pattern," "looks like."
- Don't quote the operator-style guess (single batch, one operator) as a
  certainty — the report itself stops at "shape."
- Don't paste the full report into the DM. Link to it.
