# Sybil Risk Scan — Hivemapper Network

> Independent scan generated by the GETKINETIK Bureau. **Part A** (when Solana RPC succeeds) reads on-chain state for the public HONEY SPL mint. **Part B** (optional) runs the same four geometry heuristics as `sybil-report.mjs` on a node list *you* supply. Hivemapper does not publish an unauthenticated global contributor JSON like WeatherXM's cells API or Geodnet's station list — use your own `SOLANA_RPC_URL` for reliable Part A, or attach `--nodes=` for the Geodnet-style pass.

- **Generated:** 2026-05-15T00:55:22.985Z
- **Solana RPC used:** `—`
- **HONEY mint:** `4vMsoUT2BWatFweudnQM1xedRLfJgJ7hswhcpz4xgBTy`
- **Part A (on-chain):** _skipped — see section below._
- **RPC error:** `https://api.mainnet-beta.solana.com: Too many requests for a specific RPC call`
- **Geometry heuristics were run on user-supplied file `scripts\sample-nodes.json` (see appendix).**

---

## Part A — On-chain scan skipped

The public Solana RPC endpoint rate-limited or rejected the batched `getTokenSupply` / `getTokenLargestAccounts` request. This is common for unauthenticated mainnet traffic.

**To populate Part A:** set environment variable `SOLANA_RPC_URL` to a mainnet HTTPS endpoint from Helius, QuickNode, Triton, Alchemy, etc., then re-run this script.

_Part B still ran because `--nodes=` was supplied — see appendix for geometry heuristics._

---

## Appendix — Geometry heuristics (user-supplied `nodes.json`)

_The block below is the verbatim output of `scripts/sybil-report.mjs` on the supplied file._

> Independent scan generated by the GETKINETIK Bureau using a subset of the same heuristics that drive Genesis Score v1.1. **No internal data from Hivemapper was used** — this report is built from the public node set provided to the script. For an authoritative GETKINETIK grade on any node, POST it to `/api/verify-device`.

- **Generated:** 2026-05-15T00:55:22.883Z
- **Input file:** `scripts\sample-nodes.json`
- **Nodes scanned:** 30
- **Nodes flagged:** 20 (66.7%)
- **Estimated payout exposure on flagged nodes:** $1,872

---

## How this scan works

Four conservative heuristics. Each one would, on its own, be defensible in front of an auditor — none of them rely on guessing intent.

1. **Co-location clusters** — ≥5 nodes within 50m of each other.
2. **Birth bursts** — ≥10 nodes first observed inside the same 60-minute window.
3. **Beat-rate impossibility** — claimed lifetime heartbeats per second > 1.0.
4. **Metadata twins** — ≥3 nodes sharing an exact (firmware, hardwareId) fingerprint.

Honest single-operator clusters (e.g. a coffee shop running two devices) do not trigger these thresholds. The bureau errs on the side of false negatives over false positives.

---

## 1. Co-location clusters — 1 found

### Cluster 1 — 6 nodes near (40.71280, -74.00600)
- **Payout exposure:** $72
- **Node IDs:** `node-001`, `node-002`, `node-003`, `node-004`, `node-005`, `node-006`


## 2. Birth bursts — 1 window

### Window 1 — 2025-12-01 23:00:00Z → 2025-12-02 00:00:00Z — 12 new nodes
- **Node IDs (first 10):** `burst-101`, `burst-102`, `burst-103`, `burst-104`, `burst-105`, `burst-106`, `burst-107`, `burst-108`, `burst-109`, `burst-110`, …(+2)


## 3. Beat-rate impossibility — 2 nodes

| Node ID | Lifetime beats | Age (h) | Beats/sec |
|---|---:|---:|---:|
| `rate-201` | 99,999,999 | 4537.9 | 6.12 |
| `rate-202` | 50,000,000 | 4537.9 | 3.06 |

## 4. Metadata twins — 2 fingerprint groups

### Fingerprint 1 — `firmware=v1.2.0` `hardwareId=OEM-A:01` — 6 nodes
- **Node IDs (first 10):** `node-001`, `node-002`, `node-003`, `node-004`, `node-005`, `node-006`

### Fingerprint 2 — `firmware=v0.9.1` `hardwareId=OEM-B:7F` — 12 nodes
- **Node IDs (first 10):** `burst-101`, `burst-102`, `burst-103`, `burst-104`, `burst-105`, `burst-106`, `burst-107`, `burst-108`, `burst-109`, `burst-110`, …(+2)


---

## Recommended next steps

1. Pull the flagged node IDs through your own internal fraud pipeline as a second opinion.
2. For nodes that also run GETKINETIK, POST their Proof of Origin URL to `https://getkinetik.app/api/verify-device` — the bureau will return a Genesis Score, tamper flags, and chain history.
3. For everything else, the bureau is glad to provide a deeper run (sensor coherence, cross-network signals, score-change webhooks).

Contact: **eric@outfromnothingllc.com** · https://getkinetik.app/bureau/ · https://getkinetik.app/api/docs/

---

## Methodology

- **Part A:** `getTokenSupply` + `getTokenLargestAccounts` + `getMultipleAccounts` (jsonParsed) on Solana mainnet when RPC allows. Requires a dedicated `SOLANA_RPC_URL` on many networks due to public-RPC rate limits.
- **Part B (optional):** same four conservative heuristics as `sybil-report.mjs` (co-location, birth burst, beat-rate, metadata twins) on a JSON array you provide.
- For hardware-rooted per-node attestation, POST a Proof of Origin URL to `https://getkinetik.app/api/verify-device`.

Contact: **eric@outfromnothingllc.com** · https://getkinetik.app/bureau/ · https://getkinetik.app/api/docs/
